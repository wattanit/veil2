{
  "name": "veil2-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && mkdir -p dist && cp index.html styles.css dist/ && esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --target=es2022"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
